import DatePicker, { Calendar } from "react-multi-date-picker";
import React, { useRef } from "react";

export const Cal =
    <Calendar
        numberOfMonths={3}
        disableMonthPicker
        disableYearPicker
    />;
